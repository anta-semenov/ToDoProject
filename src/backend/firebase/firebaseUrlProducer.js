//For update only changed properties unstead of uploading all state we need to calculate key for each propery
import * as actionTypes from '../constants/actionTypes'

export default function urlProducer(action, prefix='') {
  switch (expression) {
    case expression:

      break;
    default:
      
  }
}
